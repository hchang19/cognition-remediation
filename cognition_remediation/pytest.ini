[pytest]
testpaths = tests
markers =
    unit: offline, fully mocked — no network or filesystem side-effects
    integration: requires network and .env credentials
pythonpath = /Users/hchang/Desktop/cognition_interview/pypackages
